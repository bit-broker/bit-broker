{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "bbk://access",
    "type": "object",
    "properties": {
        "role": {
            "type": "string",
            "enum": ["coordinator", "contributor", "consumer"]
        },
        "context": {
            "oneOf": [
                { "type": "null" },
                { "$ref":"bbk://slug" }
            ]
        }
    },
    "required": ["role", "context"]
}
