apiVersion: v1
kind: {{ .Values.bbcontributor.volume.kind }}
metadata:
  name: {{ .Values.bbcontributor.volume.pvc.name }}
spec:
  accessModes:
    - {{ .Values.bbcontributor.volume.pvc.accessMode }}
  resources:
    requests:
      storage: {{ .Values.bbcontributor.volume.pvc.storage }}