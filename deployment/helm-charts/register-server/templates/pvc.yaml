apiVersion: v1
kind: {{ .Values.bbregserver.volume.kind }}
metadata:
  name: {{ .Values.bbregserver.volume.pvc.name }}
spec:
  accessModes:
    - {{ .Values.bbregserver.volume.pvc.accessMode }}
  resources:
    requests:
      storage: {{ .Values.bbregserver.volume.pvc.storage }}